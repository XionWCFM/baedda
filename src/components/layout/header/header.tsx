import React from 'react';

interface HeaderProps {}

const Header = ({}: HeaderProps) => {
  return (
    <header className=" flex justify-between">
      <div className="">dsadsa</div>
    </header>
  );
};

export default Header;
